<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- VISTA FORM REDISEÑADA ESTILO CHATGPT/GEMINI -->
    <record id="building_ai_chat_view_form" model="ir.ui.view">
        <field name="name">building.ai.chat.view.form</field>
        <field name="model">building.ai.chat</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <form string="Asistente IA" class="o_building_ai_chat_form" create="0">
                <header>
                    <button name="action_create_work" string="Crear Obra" type="object" class="btn-primary" invisible="state != 'ready'" icon="fa-check-circle"/>
                    <button name="action_view_generated_work" string="Ver Obra Generada" type="object" class="btn-success" invisible="not has_generated_work" icon="fa-external-link"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,ready,created"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Título del Chat..."/>
                        </h1>
                    </div>

                    <div class="o_building_chat_ui_wrapper" style="display: flex; flex-direction: column; height: 65vh; border: 1px solid #dee2e6; border-radius: 12px; background: #f8f9fa; margin-top: 20px;">

                        <!-- Área de Mensajes — OWL Widget vertical real -->
                        <div class="o_building_chat_messages_area" style="flex: 1; overflow-y: auto;">
                            <field name="chat_history_ids"
                                   widget="building_chat_messages"
                                   nolabel="1"/>
                        </div>

                        <!-- Footer con Input Area -->
                        <div class="o_building_chat_footer p-3 bg-white border-top" style="border-radius: 0 0 12px 12px;">



                            <div class="d-flex align-items-end gap-2">
                                <div class="flex-grow-1">
                                    <field name="user_input" widget="text" placeholder="Escribe tu mensaje aquí... (la IA te hará preguntas antes de generar)" nolabel="1" class="form-control border-0 bg-light shadow-none" style="min-height: 44px; padding: 10px 15px; border-radius: 22px; width: 100%; resize: none; overflow: hidden;"/>
                                </div>

                                <!-- FIX: Botón Enviar limpiado del JS atascado -->
                                <button name="action_send_message" type="object" string="Enviar" icon="fa-paper-plane" class="btn btn-primary rounded-pill px-4 py-2" title="Enviar mensaje" style="height: 44px; display: flex; align-items: center; border-radius: 22px !important;"/>
                            </div>
                        </div>
                    </div>

                    <!-- Campos ocultos necesarios -->
                    <group invisible="1">
                        <field name="has_generated_work"/>
                        <field name="generated_work_id"/>
                        <field name="user_id"/>
                    </group>
                </sheet>

                <!-- Hack para Odoo: Si un modelo hereda mail.thread y tiene adjuntos, pero no tiene chatter en la vista,
                     Odoo inyecta automáticamente una tabla cruda de adjuntos al final. 
                     Agregamos un chatter invisible para suprimir ese comportamiento. -->
                <div class="oe_chatter" invisible="1">
                    <field name="message_follower_ids" groups="base.group_user"/>
                    <field name="message_ids"/>
                </div>

                <!-- Estilos CSS para ocultar interfaz estándar de lista (headers, bordes) y simular chat -->
                <style>
                    /* Ocultar header y columnas nativas de la list */
                    .o_building_ai_chat_form .o_list_view thead,
                    .o_building_ai_chat_form .o_list_view .o_list_record_selector,
                    .o_building_ai_chat_form .o_list_view td.o_list_record_selector {
                        display: none !important;
                    }

                    /* Quitar bordes de filas para que parezca chat */
                    .o_building_ai_chat_form .o_list_view tbody tr {
                        border: none !important;
                        background: transparent !important;
                    }

                    .o_building_ai_chat_form .o_list_view tbody td {
                        border: none !important;
                        padding: 0 !important;
                    }

                    /* Scroll automático al último mensaje */
                    .o_building_ai_chat_form .o_list_view {
                        max-height: 500px;
                        overflow-y: auto;
                        display: flex;
                        flex-direction: column;
                    }
                </style>
            </form>
        </field>
    </record>

    <!-- VISTA LIST (Mantener para historial) -->
    <record id="building_ai_chat_view_tree" model="ir.ui.view">
        <field name="name">building.ai.chat.view.list</field>
        <field name="model">building.ai.chat</field>
        <field name="arch" type="xml">
            <list string="Historial de Asistente IA">
                <field name="name"/>
                <field name="create_date" string="Fecha"/>
                <field name="state" widget="badge" decoration-info="state == 'draft'" decoration-warning="state == 'ready'" decoration-success="state == 'created'"/>
                <field name="user_id" widget="many2one_avatar_user"/>
            </list>
        </field>
    </record>

    <!-- ACCION Y MENUS -->
    <record id="action_building_ai_chat" model="ir.actions.act_window">
        <field name="name">Asistente IA</field>
        <field name="res_model">building.ai.chat</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                ¡Empieza una conversación con la IA!
            </p>
            <p>
                Puedes pedirle que genere presupuestos, resuma obras o analice costos.
            </p>
        </field>
    </record>

</odoo>
